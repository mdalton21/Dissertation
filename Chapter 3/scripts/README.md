# Chapter 3 Scripts

- `data_cleaning.R`: dataset assembly and descriptive plots.
- `data_analysis.R`: main fixed-effects models and output tables/plots.
- `supplement_analysis.R`: alternative measures, ordinal quota, and RDD checks.

Scripts use repository-relative `data/`, `figures/`, and `tables/` directories.
