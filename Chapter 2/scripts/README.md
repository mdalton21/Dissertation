# Chapter 2 Scripts

- `data_cleaning.R`: preprocesses ECAV actor-target dyads and writes `ecav_net.csv`.
- `data_analysis.R`: performs network construction, centrality visualization, and SBM output generation.

Scripts now use repository-relative paths (`../data`, `../figures`).
